#QUAL IMAGEM IREMOS UTILIZAR, NESSE CASO A STABLE-ALPINE
FROM nginx:stable-alpine

#REMOVEMOS OS ARQUIVOS DE CONFIGURAÇÂO QUE VEM PADRÂO NO NGNIX
#DEIXA MAIS FACIL PARA SABER OQ ESTAMOS CONFIGURANDO NO DEFAULT.CONF
RUN rm /etc/nginx/conf.d/default.conf

#COPIAMOS O ARQUIVO DE CONFIGURAÇAO QUE FIZEMOS PARA A PASTA DO NGNIX
COPY default.conf /etc/nginx/conf.d/